<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ButtonLinearLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="ButtonLayout">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>

    </style>

    <style name="ButtonLayout.category">
        <item name="android:textSize">10dp</item>
        <item name="android:background">@null</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_margin">5dp</item>
    </style>

    <style name="ButtonLayout.num">
        <item name="android:textSize">20dp</item>
        <item name="android:background">@null</item>
        <item name="android:textColor">@color/colorNormalText</item>
    </style>

    <style name="TransactionTextView" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">30dp</item>
    </style>

    <style name="LayoutPrimary">
        <item name="android:layout_margin">20dp</item>
    </style>

    <style name="TextGroup">
        <item name="android:textSize">20dp</item>
        <item name="android:textColor">@color/colorNormalText</item>
    </style>

    <style name="TextChild">
        <item name="android:textSize">15dp</item>
        <item name="android:textColor">@color/colorNormalText</item>
    </style>


</resources>
